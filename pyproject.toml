[project]
name = "rlvr-pipeline"
version = "0.1.0"
description = "Arabic Reasoning RLVR Pipeline (TRL GRPOTrainer + QLoRA + Arabic rewards)"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.setuptools.packages.find]
where = ["src"]
